name: Generate Sponsors README
on:
  workflow_dispatch:
  
jobs:
  deploy:
    runs-on: ubuntu-latest
    # Disabled for ytclip fork
    if: false
    steps:
      - name: Disabled
        run: echo "Sponsor workflow disabled"
